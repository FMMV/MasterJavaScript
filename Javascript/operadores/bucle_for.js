'use strict'

//Bucle FOR
// Es una estructura de control que se repite varias veces.
// Un trozo de codigo que se repite un numero definido de veces.

var numeros = 100;

// Esta formado por el contador, la condicion y el incrementador.
for (var i = 0; i <= numeros; i++) {
    console.log("Vamos por el numero: " + i);
    debugger;
}